## Changes required

### 1. Firmware — `polytron-v3.yaml`

**Rinse** (line 666):
```yaml
# current
snprintf(b, sizeof(b), "DRAIN:%d, FILL:%d, WASH:%d, DRAIN:%d", d, f, r, d);
# new — pre-drain uses num_drain_min (same var, now called explicitly a 45-s default; user can tune it)
# No change to logic; the leading and trailing drain already use `d` = num_drain_min seconds.
# The 45-second intent is satisfied by changing the initial_value of num_drain_min from 5 to 1 (minutes) to a new value.
```

Wait — re-reading the user's answer: "regular rinse has pre-drained (45 sec configurable), both rinse has end drain (45 sec configurable)."

Currently:
- `Start Rinse` → `DRAIN:%d, FILL:%d, WASH:%d, DRAIN:%d` (leading + trailing drain, both using `num_drain_min` in seconds, min=1, max=15 minutes)
- `Start Rinse No Drain` → `FILL:%d, WASH:%d, DRAIN:%d` (no leading drain, trailing drain using `num_drain_min`)

The current Drain Minutes is in **minutes** (min 1, max 15, default 5 = 5 minutes). The user wants 45 **seconds** configurable for rinse drains specifically. That's a separate timing from the general "Drain Minutes" used for the standalone Drain button.

**New entity: Rinse Drain Seconds** (like Spin Drain Seconds)
- `name: "Rinse Drain Seconds"`, id: `num_rinse_drain_s`, min: 15, max: 180, step: 5, initial_value: 45
- Update `Start Rinse` seq: `DRAIN:%d, FILL:%d, WASH:%d, DRAIN:%d` where the drain values use `num_rinse_drain_s` (in seconds) directly via a DRAIN:%d step — but `run_seq` interprets tokens as seconds already.
- Update `Start Rinse No Drain` seq: `FILL:%d, WASH:%d, DRAIN:%d` where the trailing drain uses `num_rinse_drain_s`.

**New snprintf for both buttons:**
```c
// Start Rinse
const int rd = (int) id(num_rinse_drain_s).state;  // rinse drain seconds
snprintf(b, sizeof(b), "DRAIN:%d, FILL:%d, WASH:%d, DRAIN:%d", rd, f, r, rd);

// Start Rinse No Drain
const int rd = (int) id(num_rinse_drain_s).state;
snprintf(b, sizeof(b), "FILL:%d, WASH:%d, DRAIN:%d", f, r, rd);
```

---

### 2. Device web UI — `ui/www.js`

- Add `rinseDrain` to `ENTITIES.number`: `{ name: "Rinse Drain Seconds", min: 15, max: 180, step: 5, dec: 0, unit: "s" }` (line ~40)
- Add a stepper row for it in the Timing card (line ~320), right after the `rinseM` row: `stepper(N.rinseDrain, "Rinse drain", "leading + trailing")`

---

### 3. HA custom card — `ha/www/polytron/polytron-washer-card.js`

**Quick Actions two-row mobile layout (HA card only):**

Line 243 current:
```css
.qarow{display:flex;gap:8px}.qa{flex:1;text-align:center;cursor:pointer}
```
Change `.qarow` to a 4-column grid on ≥360 px, 2-column below that, so 7 items land in two rows (4+3):
```css
.qarow{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}
@media(max-width:480px){.qarow{grid-template-columns:repeat(2,1fr)}}
```
Stop All (`.r`) spans 2 columns on mobile so it stays prominent:
```css
@media(max-width:480px){.qarow .qa.r{grid-column:span 2}}
```

**Add `n_rinsedr` entity mapping** (line ~33):
```js
n_rinsedr: "number.ruang_cuci_polytron_washing_machine_rinse_drain_seconds",
```

**Add stepper in Timing section** (line ~157):
```js
${step(C.n_rinsedr, "Rinse Drain (s, leading + trailing)", "🌊", 15, 180, null, 5)}
```

---

### 4. README — `README.md`

Update the RINSE bullet (line 80–85) to document the new Rinse Drain Seconds setting and correct both rinse sequences.